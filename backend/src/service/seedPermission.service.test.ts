describe("Testing Seed Permission service", () => {
  describe("Testing seed permission function", () => {
    it.todo("Should return early if filePath is empty.");
    it.todo("Should throw error if file extension is not JSON");
  });

  describe("Testing read file callback function", ()=>{
    it.todo("Should throw error if error params is not empty")
    it.todo("Should return early if computed hash and existing hash is matched")
    it.todo("Should delete permission according to params")
    it.todo("Should insert permission according to params")
    it.todo("Should update permission according to params")
    it.todo("Should create superAdmin role on initial seed and all the permission should be added to superAdmin.")
  })
});
